var assert = require('chai');

describe('gcd greatest common denominator', function() {
  var gcd = require('./../challenges/gcd.js');

  it('should return 2 for (10, 8)', function() {
    assert.equal(1, gcd(1, 1));
  });
});
